#include <iostream>
#include <iomanip>
#include <math.h>
using namespace std;

int main() {
    int y=-2, x=1, B=1, z;

    z = (y > pow(x, 2)) && B && (pow(x, 2) + pow(y, 2) > 1) && (y , 7) && (x > -0.5) || (y <= 0);

    cout << boolalpha << "z=" << z;

    return 0;
}